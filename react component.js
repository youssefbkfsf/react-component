// Import the React library
import React from 'react';

// Define the HelloWorld component using JSX
const HelloWorld = () => {
  return (
    <div>
      <h1>Hello, World!</h1>
      <p>This is a simple React component written in JSX.</p>
    </div>
  );
};

// Export the HelloWorld component for use in other parts of your application
export default HelloWorld;
